<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.copyproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Eventdetails_Activity"></activity>
        <activity android:name=".update_profile_activity" />
        <activity android:name=".event_participation_choice_activity" />
        <activity android:name=".upload_photo_activity" />
        <activity android:name=".studentprofile_activity" />
        <activity android:name=".signup_studentinfopage" />
        <activity android:name=".signupchoice" />
        <activity android:name=".login_Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".firstscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>